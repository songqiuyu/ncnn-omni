# Core implementation

Engine/session lifecycle, package and adapter registries, capability validation,
resource admission/residency, request IDs, cancellation, status propagation, and
metrics live here. Core does not depend on ncnn or any model family.
