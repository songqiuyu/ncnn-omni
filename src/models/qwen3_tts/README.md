# Qwen3-TTS adapter

Third architecture test: text and optional reference audio conditioning,
multi-codebook/feedback generation, bounded code streaming, codec/vocoder state,
and incremental `AudioChunk` output.
