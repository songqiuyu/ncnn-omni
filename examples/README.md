# Examples

Minimal applications will be added for:

- `asr/`: file and streaming transcription;
- `vlm/`: ordered text/image inputs and streaming text output;
- `tts/`: text/reference audio input and streaming PCM output.

Examples exercise public APIs only and are not allowed to duplicate model prompt
or preprocessing logic.
