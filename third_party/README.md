# Third-party dependencies

Vendored or pinned dependencies belong here only when they are required by the
runtime and cannot be consumed cleanly through the build system. ncnn integration
must support a caller-provided package/target; a full duplicate ncnn source tree is
not required by default.
